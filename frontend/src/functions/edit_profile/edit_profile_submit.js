export const submitProfileChanges = async () => {
  const newUsername = document.querySelector("change-username-input").value
  const newPassword = document.querySelector("change-password-input").value

  // MAKE A CHECKER FOR WHEN THE INPUTS ARE EMPTY

  // const id = 

  // const data = await fetch(`http://localhost:3000/users/update/${id}`, {
  //   headers: {
       
  //   }
  // })
}